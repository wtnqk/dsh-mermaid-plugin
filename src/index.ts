// ============================================================================
// プラグインのエントリポイント (サーバー側)
// ----------------------------------------------------------------------------
// このプラグインの全面白は DSH の Web クライアント (browser) 側で行われるため、
// サーバー側 (Node) では何も処理を行わない。クライアント側の実装は
// client.tsx を参照のこと。
// ============================================================================

/**
 * プラグインの適用関数 (サーバー側)。
 * クライアントにのみ寄与するプラグインのため、実体は空とする。
 */
export function apply(): void {}
